// I'm looking specifically for id and then user_name with remaining ones
// split out id stuff
// then all non id stuff
const uniqueArr = arr.filter( ( value, index, arr ) => arr.indexOf( value ) === index );

// add up all id same with just the id filtered array
const uniqueArr = arr.filter( ( value, index, arr ) => arr.indexOf( value ) === index );

// with 2nd filtered array of non-id, add up user_name being the same
const result = uniqueArr.map( han => { 
  const entries = arr.filter( value => han. );
} );
