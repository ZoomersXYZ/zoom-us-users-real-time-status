{
  "name": "Zoomus-Meetings-Webinar-Realtime-Users-Webhook",
  "version": "0.9.1",
  "description": "",
  "private": false,
  "scripts": {
    "serve": "npx firebase emulators:start",
    "build-serve": "react-app-rewired build && npx firebase emulators:start",
    "debug": "npx firebase emulators:start --inspect-functions=12345",
    "build-debug": "react-app-rewired build && npx firebase emulators:start --inspect-functions=12345",
    "shell": "npx firebase functions:shell",
    "b-shell": "npm run shell",
    "logs": "npx firebase functions:log",
    "deploy": "npx firebase deploy",
    "b-deploy": "npx firebase deploy --only functions",
    "f-start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "author": "Saadh Shehzad",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.15.0",
    "express": "^4.17.1",
    "firebase": "^7.16.1",
    "firebase-admin": "^8.0.0",
    "firebase-functions": "^3.8.0",
    "node-sass": "^4.14.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-firebase-hooks": "^2.2.0",
    "react-scripts": "3.4.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "customize-cra": "^1.0.0",
    "eslint": "^6.8.0",
    "eslint-plugin-node": "^11.0.0",
    "firebase-tools": "^8.6.0",
    "react-app-rewired": "^2.1.6"
  }
}
