{
  "name": "Zoom.usReactBasedReal-TimeFrontend",
  "version": "0.1.0",
  "private": false,
  "dependencies": {
    "date-fns": "^2.15.0",
    "firebase": "^7.16.1",
    "node-sass": "^4.14.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-firebase-hooks": "^2.2.0",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npx firebase deploy",
    "buploy": "yarn build && yarn deploy", 
    "buildyy": "react-scripts build && npx copyfiles \"build/**\" ../backend" 
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "copyfiles": "^2.3.0"
  }
}
