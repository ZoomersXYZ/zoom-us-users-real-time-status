import React from 'react';
import EachUser from './EachUser';

const UsersList = ( users ) => (
  <div>
    <ul>
      { users.map( aUser => {} =>

      }
    </ul>
  </div>
);

export default UsersList;
